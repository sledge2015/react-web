// ================================// 2. src/pages/Dashboard/index.tsx - 仪表板页面// ================================import React, { useState, useCallback, useEffect } from 'react';import { message } from 'antd';import PortfolioPage from '../Portfolio';const Dashboard: React.FC = () => {  const [refreshing, setRefreshing] = useState(false);  const handleRefresh = useCallback(async () => {    setRefreshing(true);    try {      // 直接调用实际的刷新逻辑，不触发全局事件      // 实际的API调用应该在这里      await new Promise(resolve => setTimeout(resolve, 1000));      message.success('投资组合数据已刷新');    } catch (error) {      console.error('刷新失败:', error);      message.error('刷新失败，请稍后重试');    } finally {      setRefreshing(false);    }  }, []);  // 只监听来自外部的刷新事件  useEffect(() => {    const handleGlobalRefresh = (event: CustomEvent) => {      if (event.detail?.activeMenu === 'portfolio') {        handleRefresh();      }    };    window.addEventListener('refreshData', handleGlobalRefresh as EventListener);    return () => window.removeEventListener('refreshData', handleGlobalRefresh as EventListener);  }, [handleRefresh]);  return <PortfolioPage onRefresh={handleRefresh}  />;};export default Dashboard;